{
  "name": "tabla",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run --environment jsdom --root src/",
    "test:coverage": "vitest run --coverage --environment jsdom --root src/",
    "test:ui": "vitest --ui --environment jsdom --root src/"
  },
  "dependencies": {
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.2.0",
    "@vitest/coverage-c8": "^0.25.7",
    "@vitest/ui": "^0.25.7",
    "@vue/test-utils": "^2.2.4",
    "autoprefixer": "^10.4.13",
    "jsdom": "^20.0.3",
    "postcss": "^8.4.19",
    "tailwindcss": "^3.2.4",
    "vite": "^3.2.4",
    "vitest": "^0.25.3"
  }
}
